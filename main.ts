const multiplicacao = (valor1: number, valor2: number): number => valor1 * valor2;
console.log(multiplicacao(6,8));

const boasVindas = (nome: string, segundoNome: string) => `${nome} diz olá para ${segundoNome}`;
console.log(boasVindas('Matheus', 'Juliana'))